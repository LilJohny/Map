This module allows you to build a map of places where films of a particular year were filmed or places in a particular country where films were filmed.
Also on the map, which is the result of the module shows the population of the countries for which it is built.
The structure of the generated html file :
`<!DOCTYPE html>` # Explaining the browser how to interpret this file
`<head>` # opening a head tag that contains information that should help the browser when working with data, within this tag are meta tags that are used to store information that is intended for search engines and browsers.
`<script></script>` Used to connect Javascript scripts.
`<link/>` Used to connect styles and fonts and other external files.
`<style></style>` used to set styles of web page.
`<meta/>`  # used to store information that is intended for search engines
`</head>` # closing the head tag
`<body></body>` # Used to store main content
Conclusion: 
This map displays  statistical data about population of countries and data about particular films, according to given input data.